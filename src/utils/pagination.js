export class PaginationSetting {
  constructor(page, pageSize, total, size, pagerCount) {
    this.page = page
    this.pageSize = pageSize
    this.total = total
    this.size = size
    this.pagerCount = pagerCount
  }
}